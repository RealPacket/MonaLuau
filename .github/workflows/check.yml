name: CI

# Controls when the workflow will run
on:
    push:
        branches: ["main"]
    pull_request:
        branches: ["main"]

    workflow_dispatch:

jobs:
    check:
        # The type of runner that the job will run on
        runs-on: ubuntu-latest

        # Steps represent a sequence of tasks that will be executed as part of the job
        steps:
            # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
            - uses: actions/checkout@v4

            - name: Setup Node.js environment
              uses: actions/setup-node@v4.0.3

            - name: Check prettier
              run: |
                  npx prettier --check .
                  # check if the exit code is 1
                  # if the exit code is 0, it means there are no formatting issues
                  if [ $? -ne 0 ]; then
                      # if the exit code is 1, it means there are formatting issues,
                      # so format the code and show the diff
                      npx prettier --write .
                      git diff
                  fi

    # fix:
    #     runs-on: ubuntu-latest
    #     permissions:
    #         contents: write
    #     steps:
    #         - uses: actions/checkout@v3
    #         - uses: actions/setup-node@v3
    #         - run: npx prettier --write .
    #         - run: git add --all
    #         - run: git diff --exit-code
    #         - run: git commit -m "Fix formatting"
